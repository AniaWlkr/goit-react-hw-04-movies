import React from 'react';
import NavBar from '../NavigationBar/NavigationBar';

const AppBar = () => {
  return (
    <div>
      <header>
        <NavBar />
      </header>
    </div>
  );
};

export default AppBar;
